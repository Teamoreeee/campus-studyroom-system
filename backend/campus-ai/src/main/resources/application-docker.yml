spring:
  datasource:
    url: jdbc:mysql://campus-mysql:3306/campus_studyroom?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  cloud:
    nacos:
      discovery:
        server-addr: campus-nacos:8848
      config:
        enabled: false
  data:
    redis:
      host: campus-redis
      port: 6379
      database: 0

logging:
  level:
    root: info
